const DEFAULT_CANVAS_WIDTH = 500; // Default canvas width
const DEFAULT_CANVAS_HEIGHT = 500; // Default canvas width
const CENTER_X = DEFAULT_CANVAS_WIDTH / 2; // Default canvas center-x
const CENTER_Y = DEFAULT_CANVAS_HEIGHT / 2; // Default canvas center-y

const DEFAULT_GRAPH_WIDTH = 300; // Default graph width
const DEFAULT_GRAPH_HEIGHT = 300; // Default graph height
const DEFAULT_DIVISIONS = 10; // Default graph divisions

const DEFAULT_POINT_RADIUS = 6;
